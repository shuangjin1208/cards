## Packages
framer-motion | Essential for the 3D card flip and swipe gesture animations
lucide-react | Icons for the UI
date-fns | Date formatting for last studied times
canvas-confetti | For completion celebration (optional but highly recommended for polished feel)
@types/canvas-confetti | Types for confetti

## Notes
Mobile-first layout implemented as a centered column on desktop
API routes follow the provided shared/routes.ts manifest
Study sessions are persisted automatically via the /api/decks/:deckId/session endpoint
